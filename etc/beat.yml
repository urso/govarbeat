################### Govarbeat Configuration Example #########################

############################# Govarbeat ######################################

govarbeat:
  remotes:
    filebeat:
      host:
        - localhost:6001

    packetbeat:
      host:
        - localhost:6002

    topbeat:
      host:
        - localhost:6003
